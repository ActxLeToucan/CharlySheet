version: "3.8"
name: charlysheet-frontend

services:
  web:
    build: .
    ports:
      - "80:80"
    environment:
      VITE_API_HOST: localhost:3000/v1
      VITE_SOCKETIO_HOST: localhost:3000
      VITE_SOCKETIO_PATH: /
    restart: always